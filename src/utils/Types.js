export const MessageType = {
    file : null,
    text : null,
}

export const FileJobType = {
    upload : null,
    download : null,
}

export const FileJobStatus = {
    progressing : null,
    completed : null,
    failed : null,
}
